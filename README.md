# astrodar

`astrodar` 是一个本地计算、交互式终端天文历程序。它从时间尺度、解析轨道理论和坐标变换出发计算天文与历法数据，而不是查询在线历表。

当前程序聚焦于太阳、月球、节气、八字年/月/日/时柱，以及柏林和北京两个独立地区的本地显示。

## 平台与依赖

- macOS 与 Linux；需要 POSIX 时间接口和 IANA 时区数据库。
- C++17 编译器。
- [FTXUI](https://github.com/ArthurSonzogni/FTXUI) 开发库，用于终端界面。

macOS/Homebrew：

```sh
brew install ftxui
make
make run
```

Linux 请先通过发行版软件包或 FTXUI 源码安装 FTXUI，再运行：

```sh
make
make run
```

默认 `Makefile` 会在 macOS 上通过 Homebrew 自动定位 FTXUI；若库安装在非标准目录，可以指定：

```sh
make FTXUI_PREFIX=/path/to/ftxui
```

### 地点配置

地点面板分为两种互斥类型：

- **时区**：仅保存 IANA 名称，例如 `Europe/Berlin`。程序从系统 IANA `zone1970.tab` 或 `zone.tab` 读取代表性坐标；日出日落等数据标为该时区的参考值。
- **坐标**：保存用户指定的地点名称、纬度和经度，不绑定法定时区；时间与太阳活动按真太阳时计算。

配置文件为 `locations.json`。Linux 路径是 `$XDG_CONFIG_HOME/astrodar/locations.json`（未设置时为 `~/.config/astrodar/locations.json`）；macOS 路径是 `~/Library/Application Support/astrodar/locations.json`。文件只保存原始选择，不保存从 IANA 数据库派生的参考坐标，并采用临时文件加原子重命名写入。

按 `A` 打开“添加地点”。确认时会校验输入、立即加入概览并写入配置；概览最多保留四个地点面板。按 `E` 打开编辑页：上下选择地点，`Enter` 选中后可用上下调整顺序，退格删除并经确认后写入配置。

## TUI 使用方式

| 按键 | 功能 |
| --- | --- |
| `Tab` | 在“概览”和“天文数据”页之间切换 |
| `+` / `-` | 以 100 ms 调整刷新周期，范围为 100–10,000 ms |
| `?` | 打开或关闭术语说明弹窗 |
| `F` | 打开或关闭坐标查询弹窗 |
| `A`（仅概览） | 打开添加地点弹窗 |
| `E`（仅概览） | 打开地点编辑：排序、删除 |
| `Esc` | 关闭术语说明弹窗；无弹窗时退出 |
| `q` | 退出 |

### 概览页

- BER 与 BEI 两个独立时区卡片；每张卡片按自身时区显示本地时间、刻和八字年/月/日/时柱。
- 时区卡片显示来自系统 IANA 数据库的参考坐标；日出日落等太阳活动是该参考坐标的结果，而非整个时区的统一值。
- 概览最多显示四个地点，按两列两行排列；页面按实际内容高度布局，不会将地点卡片纵向拉伸至终端底部。
- 太阳事件区按地点坐标显示日出、太阳中天、日落，以及民用、航海、天文晨昏蒙影的本地时间。
- 矮终端（低于 32 行）会自动将太阳活动时间线折叠为两列；高度充足时维持完整单列时间线。

### 天文数据页

- 上方：UTC、Unix 时间戳、JD、MJD、JD(TT)。
- 中间左栏：太阳和月球的文字数据块，分别显示位置、距离和照明等量。
- 中间右栏：连续计算的月相盘，使用 `░` 和 `▓` 绘制暗面与亮面；圆盘先在逻辑坐标中计算，再在输出阶段以横向字符倍率做栅格补偿。
- 下方：当前/下一节气与本年八个主节气的 UTC 时刻。

显示层通过 [localization.cpp](localization.cpp) 集中管理中文文案。当前仅提供中文 UI；标准缩写如 UTC、TT、JD、MJD、AU 与 km 保留。

### 坐标查询弹窗

按 `F` 打开覆盖式坐标查询。查询不使用法定时区，而是以经度和均时差计算真太阳时；适用于任意经纬度地点的时间、干支与太阳活动查询。

| 按键 | 功能 |
| --- | --- |
| `↑` / `↓` | 选择输入行；在自定义 UTC 编辑中调整当前时间段 |
| `Tab` | 切换当前行的模式：时间来源、纬度 N/S 或经度 E/W |
| `Enter`（自定义 UTC） | 进入或退出分段时间编辑；`←` / `→` 选择年、月、日、时、分、秒 |
| 数字、小数点、度分秒符号、退格 | 编辑 JD、纬度或经度字段 |
| `F` / `Esc` | 关闭查询弹窗 |

时间来源可选实时 UTC、自定义 UTC 与 `JD(TT)`。实时 UTC 的时间行只读，焦点导航会自动跳过它。选择自定义 UTC 时会以当前 UTC 初始化；其年、月、日、时、分、秒采用下划线分段编辑。调整月份或年份会将日期截断到目标月的合法末日，调整日期、时分秒会自动跨月、跨年。日期遵循完整公历闰年规则：4 年一闰，世纪年仅在能被 400 整除时为闰年。JD 输入直接解释为 TT 儒略日，适合可复现的天文计算。

查询输出的时间是按经度与均时差计算的真太阳时；下方同时给出干支、太阳位置、太阳活动时间线和月球位置。纬度和经度支持以下四种形式：

```text
52.5200
52°31′12″
52 31 12
52d31m12s
```

内部统一转换为十进制度。纬度范围为 `0–90°`，经度范围为 `0–180°`，度分秒中的分、秒范围均为 `0–59.999…`；北/南、东/西字段决定最终正负号。

## 计算模型与时间尺度

### 时间

- Unix 时间与 UTC 公历时间。
- JD、MJD、JDN。
- UTC → TT：内置 1972-01-01 起至 2017-01-01 的闰秒表。2017 年后暂按 `TAI−UTC = 37 s` 处理；若未来再出现闰秒，需要更新表。
- TT → UTC：用于将节气天文瞬间显示为 UTC。
- ELP82B 的原始理论以 TDB 表示自变量；当前以 `TT ≈ TDB` 代入。两者的周期差为毫秒量级，不是当前月出没分钟级误差的来源。
- 地平坐标和月出没中的地球自转时间当前以 UTC 近似 UT1；未接入每日地球定向参数（`UT1−UTC`、极移）。这通常造成秒级而非分钟级影响。

UTC 早于 1972 年的 TAI−UTC 关系不属于整秒闰秒表范围，因此当前明确不支持该转换。

### 岁差与章动

[earth_orientation.cpp](earth_orientation.cpp) 集中提供太阳、月球共用的地球定向近似：IAU 2006 平均黄赤交角、一个标量黄经岁差项，以及四个主项的黄经/黄赤交角章动。

这里必须区分“坐标标签”和“完整坐标框架变换”。ELP82B 原始输出是**地心 J2000 平黄道**球坐标；当前程序将其黄经加上标量黄经岁差和黄经章动，再以平均黄赤交角加章动黄赤交角换算赤经赤纬。黄纬仍采用 ELP 的原值，并没有实施完整三维岁差—章动旋转矩阵。因此界面中的“日期分点/视”可理解为适合当前显示和事件计算的近似日期坐标，不应等同于完整 IAU 2000B/2000A 或 IAU 2006 CIO 框架。

这不是 ELP82B 地心轨道项是否“正确”的问题，而是从 J2000 理论坐标到观测日期参考系时所采用的定义与阶数。后续可在同一接口中替换为 IAU 2000B 77 项章动和完整旋转矩阵，并接入地球定向参数。

### 真太阳时

[solar_time.cpp](solar_time.cpp) 实现视地方时（常称“真太阳时”）：

```text
真太阳时 = UTC + 经度 × 4 分钟 + 均时差
```

东经为正、西经为负。该时间不依赖法定时区，适合作为后续任意坐标查询的时间与干支基准；BER/BEI 预置地点页面仍显示各自的民用时区时间。

### 太阳

[solar_model.cpp](solar_model.cpp) 使用由 [IMCCE VSOP87D Earth](https://ftp.imcce.fr/pub/ephem/planets/vsop87/VSOP87D.ear) 数据机械转换的本地 2,425 项地球 `L/B/R` 级数表 [vsop87d_earth.inc](vsop87d_earth.inc)。计算流程为：

1. 以 TT 儒略日计算地球在日期分点黄道坐标中的日心经度、纬度与距离；
2. 反向得到太阳地心几何位置；
3. 应用主项章动与年周光行差，得到太阳视黄经；
4. 转换得到赤经和赤纬。

均时差目前使用低阶解析式，仅作显示，不参与节气或历法判断。

### 节气

二十四节气由太阳视黄经的方程直接定义：

```text
λ☉ = 15° × n,  n = 0..23
```

[solar_terms.cpp](solar_terms.cpp) 在目标附近建立十天区间，并以二分法求根。程序显示当前节气、下一节气和本年八个主节气：立春、春分、立夏、夏至、立秋、秋分、立冬、冬至。节气瞬间首先以 TT 得到，再转换为 UTC 显示。

### 月球

[elp82b_model.cpp](elp82b_model.cpp) 使用编译进二进制的完整 ELP2000-82B 周期/Poisson 系数。原始 `ELP1` 至 `ELP36` 文本表经 [tools/embed_elp82b.py](tools/embed_elp82b.py) 转换为 [elp82b_embedded.cpp](elp82b_embedded.cpp)，运行时不读取外部系数文件。它求出月球地心 J2000 平黄道经度、纬度与距离；[moon_model.cpp](moon_model.cpp) 将其转换至日期分点，并派生地心赤经赤纬、赤道地平视差、视半径，以及有符号日月黄经差、绝对角距和照明比例。

月相方向由有符号日月黄经差判断：正值为渐盈，负值为渐亏。天文数据页的月相盘使用同一受光方向，不使用 emoji 或位图。

ELP82B 系数来自 [IMCCE ELP82B](https://ftp.imcce.fr/pub/ephem/moon/elp82b/)。查询页已基于观测者经纬度计算站心赤道坐标、北起顺时针方位角、真仰角，以及年鉴等价的月出、中天、月没。下面的定义决定了读数与不同年鉴能否直接比较。

| 界面量 | 定义 |
| --- | --- |
| 地心黄道经度、纬度、地月距离 | 地心至月心的球坐标；距离为地心—月心距离。 |
| 地心赤经、赤纬 | 由当前的近似日期黄道坐标换算；赤经以时显示，`1 h = 15°`。 |
| 赤道地平视差 | `asin(地球赤道半径 / 地心月距)`，是地心量；并非某一站点实际赤纬/赤经改正的总角距。 |
| 视半径 | `asin(月球半径 / 地心月距)`；未包含地形或大气放大效应。 |
| 站心赤道坐标 | 以输入经纬度和 WGS84 式椭球地球几何把月球由地心改正到观测者位置。查询页当前未提供海拔输入，海拔按 0 m 处理。 |
| 方位角、真仰角 | 方位角从真北起向东量取（北 `0°`、东 `90°`）；真仰角为纯几何高度，显示值不叠加实时大气折射。 |

月出、月没采用常用年鉴的“月心标准高度”定义。在程序已做站心视差改正的前提下，求根条件为：

```text
站心月心真仰角 = −(34′ + 月球视半径)
```

其中 `34′` 是月光平均折射的传统常数，`s` 是当时视半径。部分年鉴把条件写成地心高度 `−34′ − s + π`，其中 `π` 为地平视差；等价地，也可表述为月心位于几何地平线下 `34′ + s − π`。两种写法分别在地心与站心坐标中表达同一件事，**不可在站心结果上再次扣除视差**。中天定义为该当地日窗口内真仰角的最大时刻。程序不处理地形遮挡、实测气压温度、极移或精确 UT1，因此月出/中天/月没目前定位为约 `±5` 分钟的实用结果，而非观测台发布级预报。

查询页按输入经度的真太阳时组织日期与显示；有些年鉴将月出没表固定在某条标准经线的平太阳时下，同一物理瞬间会因时间坐标约定而显示为不同钟点。比较时应先统一时间尺度和经度约定，而不是把这种钟点差异误判为轨道算法误差。

天平动、月面中心经纬度、转轴倾角和亮缘方位角仍待加入；当前模型不能据此宣称已完成高精度农历定朔、食或月面物理量的全部链路。

## 历法与干支规则

### 日柱与时柱

- 日柱以各地点自身的本地公历日期转换为 JDN 后计算。
- 时支按两个小时为一个时辰计算；时间行额外显示“子初一刻”等刻位。
- 时干由日干与时支计算。

因此 BER 与 BEI 在本地跨日或跨时辰时可得到不同的日柱、时柱；它们不共享北京的历法结果。

### 八字年柱

采用四柱常用的“立春瞬间换年”规则，不采用农历正月初一换年。

立春定义为太阳视黄经 `λ☉ = 315°`。为了避免为每次年柱显示重复求根，程序使用等价的直接判断：一月恒在立春前；二月仅在 `λ☉ < 315°` 时仍属上一八字年。该化简不改变传统规则。

### 八字月柱

八字月柱不按农历月，也不按中气，而按十二个“节”切换。传统“甲己之年丙作首”等口诀在程序中被写为等价数学式：

```text
m = floor(mod(λ☉ − 315°, 360°) / 30°)
月支索引 = (2 + m) mod 12
月干索引 = (2 + 2 × (年干索引 mod 5) + m) mod 10
```

`m = 0` 为寅月，`m = 11` 为丑月。这样只需当前太阳视黄经即可计算月柱；节气求根仍用于显示节气发生时刻。

## 当前能力

| 范围 | 状态 | 说明 |
| --- | --- | --- |
| 交互 TUI | 已完成基础版 | 页面、刷新率控制、术语弹窗、本地化文案层 |
| 地点面板 | 已完成基础版 | 最多四个面板；IANA 时区参考地点与自定义真太阳时地点；支持添加、排序、删除和本地持久化 |
| 坐标查询 | 已完成基础版 | 真太阳时、实时 UTC/自定义 UTC/JD(TT)、分段日期编辑、坐标化干支、太阳与月球数据 |
| UTC / TT / JD | 已完成基础版 | 1972 年起的闰秒表转换 |
| 本地时区 | 已完成 | BER 与 BEI 使用 IANA 时区名独立显示 |
| 太阳位置 | 已完成 | VSOP87D 地球级数、主项章动与光行差 |
| 二十四节气 | 已完成基础版 | 太阳视黄经求根、UTC 输出 |
| 月球地心位置与月相 | 已完成 ELP82B 地心层 | 完整本地周期项已内嵌；月相为日月黄经差派生量，朔望/四相的专用求根尚未加入 |
| 月球站心、地平与出没 | 已完成基础版 | 查询页按输入经纬度计算站心赤经赤纬、方位角、真仰角及年鉴标准月出/中天/月没；当前目标为约 ±5 分钟 |
| 八字年/月/日/时 | 已完成基础版 | 采用本文明确的立春与节气月规则 |
| 日出、日落、太阳中天与晨昏蒙影 | 已完成基础版 | 使用地点经纬度、太阳赤纬、均时差，以及 `−0.833°`、`−6°`、`−12°`、`−18°` 高度阈值 |
| 农历、朔望、闰月 | 未完成 | 已具备地心日月位置核心输入；仍需朔望/中气求根、历法规则版本和完整回归验证 |
| 月面物理量 | 未完成 | 需要天平动和亮缘方位角模型 |
| 行星、食与掩星 | 未完成 | 需要其余行星理论及更完整的月球/地球模型 |

## 精度与边界

- 节气求根的数值误差很小，但总体精度由太阳模型、章动近似和 UTC↔TT 数据决定。
- 未来 UTC 标记受到未来闰秒不可预知的限制；TT 中天文计算本身不受这一限制。
- ELP82B 的完整周期项已随二进制编译；这提高了地心月球轨道项的完整性，但不自动补足坐标参考系、地球定向、观测条件或历法定义。
- 月球的 J2000 → 日期坐标处理目前是标量岁差加低阶章动，而非完整 IAU 旋转；赤经、赤纬和地平结果应按这一近似层级理解。
- 站心地平量以 UTC 近似 UT1、海拔为 0 m，未用极移、地形和逐时气象折射。月出、月没采用固定 `34′` 折射常数，适用目标为约 ±5 分钟。
- 月球 ELP82B 地心位置可用于后续朔望求根的核心输入；农历、月食和日食仍需更完整的地球定向、站心视差、历法规则及验证链路。
- 历史农历和民俗纪年存在地区、时区与规则版本差异；不能由一套现代规则自动覆盖。

## 后续计划

1. 在现有日月位置之上加入朔、望和四相的数值求根，并定义输出采用的 TT、UTC 与历法日界。
2. 实现明确版本的中国阴阳历：定朔、中气、月序、闰月、时区与历法日界必须作为规则的一部分固定下来。
3. 将月球 J2000 → 日期坐标升级为完整岁差—章动旋转，接入 IAU 2000B/更高阶章动和地球定向参数；再评估站心与出没精度。
4. 加入月面物理量：天平动、月面中心经纬度、转轴倾角和亮缘方位角。
5. 接入其余 VSOP87 行星项，提供地心位置、合冲、大距和逆行。
6. 建立对权威年历或数值星历的回归测试，分别记录地心、站心、地平和事件时刻的参考系、时间尺度、适用年份与误差。

## AI使用说明

Supported by OpenAI Codex
